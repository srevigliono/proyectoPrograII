<!DOCTYPE html>
<html lang="es">

	<!-- Includes-->
	<%- include('./partials/head.ejs') %>
	<%- include('./partials/header.ejs') %>
			

	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title prompt-semibold">Ingresar</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<h2 class="col-12"> <i>///////////////////////////////////////// 

				<form action="/users/login" method="post" class="prompt-light">
			
					<div >
						<label for="email">Usuario: </label>
						<input id="email" type="email" name="email" required
						value="<%= locals.oldData && oldData.email ? oldData.email : null %>">
					
					<% if (locals.email && errors.email) { %>
					<div class="error-msg">
						<%= errors.email.msg %>                                                        </div>                            
					<% } %>
					<div>
					
					<div>
						<label for="password">Contraseña: </label>
						<input id="password" type="password" name="password" required
						value="<%= locals.oldData && oldData.password ? oldData.password : null %>">
					
					<% if (locals.errors && errors.password) { %>
                    <div class="error-msg">
                        <%= errors.password.msg %>                                                        </div>                            
                    <% } %>
					<div>
						<label> Recordar información de incio sesión:</label>
						<input type="checkbox" name="recordar" value="si"> 
					</div>	
						<button type="iniciar sesión" href="/">Iniciar sesión</button></p>
					</form>
			</h2>

		

	<%- include('./partials/footer.ejs') %>
